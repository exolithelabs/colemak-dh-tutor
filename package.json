{
  "name": "colemak-dh-tutor-desktop",
  "private": true,
  "version": "0.1.1",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "tauri": "tauri",
    "sidecar:windows": "powershell -NoProfile -ExecutionPolicy Bypass -File scripts/build-sidecar.ps1"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4"
  }
}
